server {
    listen 80;
    root /usr/share/nginx/html;

    # Client-side routes (/client/42) are served by index.html.
    location / {
        try_files $uri /index.html;
    }

    location /static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
